cmake_minimum_required(VERSION 3.5)
project(ftsm_base_test)
set (CMAKE_CXX_STANDARD 11)

find_package(PkgConfig)
find_package (Threads)
pkg_check_modules(JSONCPP jsoncpp)
link_libraries(${JSONCPP_LIBRARIES})

include_directories(
    /opt/ropod/ftsm/cppftsm/include
    include
    ${JSONCPP_INCLUDE_DIRS}
)

add_executable(test
    src/test.cpp
    /opt/ropod/ropod_common/ropodcpp/ftsm/src/ftsm.cpp
    /opt/ropod/ftsm/cppftsm/src/ftsm_base.cpp
)
target_link_libraries(test ${LIBRARIES} ${CMAKE_THREAD_LIBS_INIT})
